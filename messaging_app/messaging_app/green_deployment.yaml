apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-green-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: django
      version: green
  template:
    metadata:
      labels:
        app: django
        version: green
    spec:
      containers:
        - name: django-container
          image: your-dockerhub-username/messaging-app:green
          ports:
            - containerPort: 8000
